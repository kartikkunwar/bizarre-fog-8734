import { Route,Routes } from "react-router-dom"

export const AllRoutes=()=>{
       <Routes>
        <Route to='/' element></Route>
        <Route to='/' element></Route>
        <Route to='/' element></Route>
       </Routes>
}